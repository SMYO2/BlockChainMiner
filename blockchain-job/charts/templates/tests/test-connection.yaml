apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "blockchain-job.fullname" . }}-test-connection"
  labels:
    {{- include "blockchain-job.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "blockchain-job.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

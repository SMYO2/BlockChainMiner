apiVersion: v1
kind: Service
metadata:
  name: {{ include "blockchain-job.fullname" . }}
  labels:
    {{- include "blockchain-job.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "blockchain-job.selectorLabels" . | nindent 4 }}
